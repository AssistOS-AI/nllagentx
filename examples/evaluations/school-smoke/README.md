# School smoke evaluation

This suite exercises isolated agent/task creation, deterministic replay, artifact retention and runtime reporting. Use `--invoke-agent` to include IntentJS and LongTextJS authoring by Codex; omit it for an infrastructure-only replay.
